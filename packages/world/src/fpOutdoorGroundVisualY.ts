/**
 * Y position (world metres, scene root) of the infinite outdoor ground mesh in FP sessions.
 * Keep in sync with `attachFpSessionEnvironment` in the client.
 */
export const FP_OUTDOOR_GROUND_VISUAL_Y = -0.02 as const;
